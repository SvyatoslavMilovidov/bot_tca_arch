# ⚙️ Пакет `service`

## 📋 Оглавление
1. [Описание пакета](#описание-пакета)
2. [Внутренняя архитектура](#внутренняя-архитектура)
3. [Чек-лист добавления элементов](#чек-лист-добавления-элементов)

---

## Описание пакета

### 🎯 Назначение
Пакет `service` содержит **бизнес-логику приложения**, организованную по **доменам** (предметным областям). Каждый домен представляет отдельную область ответственности.

### 🏗️ Принципы организации
- **Разделение по доменам:** каждый домен = отдельный пакет
- **Единая ответственность:** каждый сервис решает одну задачу

### 📁 Что содержит
- **Пакеты доменов:** db, payment, email, ml
- **Сервисные классы:** бизнес-логика внутри доменов
- **Методы:** конкретные операции

---

## Внутренняя архитектура

### 📂 Структура пакета

```
service/
├── __init__.py           # Базовые классы сервисов
├── db/                   # Работа с базой данных
│   ├── __init__.py       # Описание пакета db
│   ├── user_service.py   # Управление пользователями
│   └── company_service.py # Управление компаниями
├── payment/              # Платежные операции
│   ├── __init__.py       # Описание пакета payment
│   └── billing_service.py # Биллинг и транзакции
└── email/                # Работа с почтой
    ├── __init__.py       # Описание пакета email
    └── sender_service.py # Отправка писем
```

### 🗄️ Пример создания пакета сервисов: service/db/__init__.py

```python
"""
Пакет сервисов для работы с базой данных.

Содержит сервисы для управления:
- Пользователи и их профили
- Компании и их настройки  
- Вакансии и требования
"""
```

### 👤 Пример создания сервиса: service/db/user_service.py

```python
"""
Сервис для управления пользователями системы.

Предоставляет методы для:
- Создания и обновления профилей пользователей
- Управления настройками и предпочтениями
"""

class UserService:
    """Сервис для управления пользователями."""
    
    async def create_user(self, telegram_id: int, username: str = None):
        """
        Создать нового пользователя в системе.
        
        Args:
            telegram_id: ID пользователя в Telegram
            username: Username в Telegram (опционально)
            
        Returns:
            dict: Данные созданного пользователя
            
        Examples:
            >>> user_service = UserService()
            >>> result = await user_service.create_user(123456789, "john_doe")
            >>> print(result["user_id"])
        """
        # Реализация метода
        pass
```

### 📝 Правила документации сервиса

Каждый пакет сервисов должен содержать в `__init__.py`:
1. **Описание назначения** пакета
2. **Список областей** которыми занимается пакет

Каждый модуль сервиса должен содержать:
1. **Описание назначения** сервиса
2. **Список функций** которые предоставляет

Каждый метод сервиса должен содержать:
1. **Описание назначения** метода
2. **Args:** описание параметров с типами
3. **Returns:** описание возвращаемого значения
4. **Examples:** пример использования

---

## Чек-лист добавления элементов

### ✅ Добавление нового домена сервисов

- [ ] **Определить предметную область** домена
- [ ] **Создать пакет** `service/{domain_name}/`
- [ ] **Создать __init__.py** с описанием домена
- [ ] **Спланировать сервисы** внутри домена

### ✅ Добавление нового сервиса в домен

- [ ] **Определить ответственность** сервиса
- [ ] **Создать класс** сервиса
- [ ] **Определить публичные методы** - основной API
- [ ] **Написать docstring** для класса и методов
- [ ] **Добавить примеры** использования

### ✅ Добавление метода в сервис

- [ ] **Определить назначение** метода
- [ ] **Выбрать сигнатуру** - параметры и возвращаемые значения
- [ ] **Написать docstring** с примерами
- [ ] **Добавить валидацию** входных параметров
